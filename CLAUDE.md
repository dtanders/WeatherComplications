This is a WearOS Complications project written in Kotlin targeting the latest version. It gets current and daily weather data from Open Meteo and exposes it as complications of the appropriate type (ie When daily data is available, create a RANGED_VALUE) to be included on watch faces. Include current Relative Humidity, Dewpoint, Apparent Temperature, Snow Depth, Visibility, UV Index, and AQI using best match weather model. When daily data is available, create a range complication. Retrieve data no more than once per hour. Get the location from the system if possible.